server:
  port: 8001

spring:
  application:
    name: cloud-payment-service
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource            # 当前数据源操作类型
    driver-class-name: org.gjt.mm.mysql.Driver              # mysql驱动包
    url: jdbc:mysql://103.196.21.253:3306/springcloud-2020?serverTimezone=GMT%2B8&useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: root
    password: z?guwrBhH7p>
    druid:
      validation-query: select 1
      test-while-idle: true

mybatis:
  mapperLocations: classpath:mapper/*.xml
  type-aliases-package: com.lisen.springcloud.entities    # 所有Entity别名类所在包